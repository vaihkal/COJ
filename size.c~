#include <stdio.h>
int main(){
	//long long int a, b, c, i; 
	unsigned n;//unsigned -> sin signo,//long long -> 
	//float h;
	//char f, D[12];
	unsigned int o, x;
	n=4294967290;
	//x=sizeof(o);
	printf("%u\n", n);
	//a=1; b=1;
//	printf("%lld, %lld, ", a, b);
	/*for(i=1; i<n; i++){
		c=a+b;
		b=a;
		a=c;
		printf("%lld\n", c);
		}*/
	return 0;
	
	}
